# set
set(target nndeploy)
set(src)
set(obj)
set(lib)
set(exe)
set(dep)
set(link)

# include
include_directories(ROOT_PATH)

# lib

add_library(${lib_target} ${obj})

# path

# depend

# link

# install

# pack

# unset
unset(lib_target)
unset(lib_src)
unset(obj)
unset(lib)
unset(dep)
unset(link)

