#include "nndeploy/track/boxmot/boxmot_convert.h"

// Template implementations are in the header (boxmot_convert.h).
// This .cc file exists to ensure the header compiles correctly and
// to provide a translation unit for any future non-template helpers.

namespace nndeploy {
namespace track {

// All conversion functions are template-based and defined in the header.
// No additional non-template code needed at this time.

}  // namespace track
}  // namespace nndeploy
